# Alta Vibración — environment template. Copy to .env.local and fill in.
# All values are public (NEXT_PUBLIC_*) — inlined into the client bundle, no secrets.

# WhatsApp business number for the "Agenda tu cita" CTA / click-to-chat.
# wa.me format: country code first, digits only, no '+' or spaces.
# Open question — see epic oscar-ospina/saas-planner#16. Falls back to a dev placeholder if unset.
NEXT_PUBLIC_WHATSAPP_NUMBER=573000000000
